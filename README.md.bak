# 致敬传奇AI项目

> 一个多角色 AI 模拟 + 故事生成项目：让一批有血有肉的"人"在同一个世界里各自生活、互动，再把他们的生活记录转成小说。
> 当前主线：**明远中学 302 寝室**（六名高中室友 + 班主任 + 物理张老师 + 篮球队教练）。
> 核心能力：任何角色设定都能跑、任何日志都能转故事、剧情出现新角色自动生成人设。

---

## 一、项目是干什么的

1. **模拟**：读取角色设定（JSON），每个角色由 AI 驱动，在校园场景里独立思考、行动、移动、互相发消息。
2. **记录**：把每个角色每轮"干了什么"按原始格式落盘成日志（单步记录：思考/记忆/动作/场景状态）。
3. **自动造人**：剧情中出现新角色时，自动生成详细人设并加入模拟，后续按人设行动。
4. **转换**：把日志自动整理成小说（群像叙事 / 轻小说 / 小说式排版，含人物关系网、心理描写）。

---

## 二、3 秒上手

```powershell
# 用 Codex 自带的 Python（项目内脚本都在 D 盘，用绝对路径）
$PY = "C:\Users\萌\Documents\Codex\2026-08-15\bang\work\venv\Scripts\python.exe"

# 1) 跑 3 轮校园模拟（每轮 6~9 个角色各自行动，剧情出现新角色会自动生成）
& $PY "D:\致敬传奇AI项目\记录\程序\run_school.py" 3

# 2) 把刚生成的日志转成群像小说
& $PY "D:\致敬传奇AI项目\记录\程序\story_converter.py" --school "D:\致敬传奇AI项目\记录\原始日志\明远中学_原始记录_xxx.txt"

# 3) 新建/补全一个角色（本地秒出，不满意再 AI 扩写）
& $PY "D:\致敬传奇AI项目\记录\程序\character_generator.py" generate "张伟" --info "17岁，懒散，爱打游戏" --enhance "多写具体事例"
```

---

## 三、增强版架构（参考水濑祈）

参考"智能体水濑祈"的运行原理，项目增加了以下核心系统：

| 系统 | 文件 | 功能 |
|------|------|------|
| 记忆系统 | `enhanced_memory.py` | SQLite持久化、记忆压缩、长期事实 |
| 情绪系统 | `emotion_system.py` | 多维度情绪跟踪、衰减机制 |
| 生活状态 | `life_state.py` | 作息时间表、活动状态机 |
| 思考日志 | `thinking_logger.py` | 内心思考记录、思考分析 |
| 增强引擎 | `enhanced_engine.py` | 整合所有系统的完整引擎 |

详见 [ENHANCED_README.md](ENHANCED_README.md)

---

## 四、实时互动监控（新功能！）

想**实时观看**角色们的互动吗？现在可以像看直播一样了！

### 快速开始

```powershell
# 进入项目目录
cd "D:\致敬传奇AI项目"

# 方式一：终端实时监控（推荐，彩色输出）
python realtime_monitor.py 3        # 运行3轮
python realtime_monitor.py          # 无限运行（Ctrl+C停止）

# 方式二：网页实时监控（浏览器查看）
python realtime_monitor.py --html   # 生成HTML监控页面

# 方式三：演示脚本（交互式选择）
python demo_monitor.py
```

### 监控效果

```
╔════════════════════════════════════════════════════════════╗
║  第 1 轮 | 第1天 14:00                                      ║
╚════════════════════════════════════════════════════════════╝

【陈子墨】 教学楼-高二(3)班
  💭 思考【今天月考成绩要出来了，有点紧张...】
  📝 更新记忆【月考成绩即将公布】
  🎬 我要前往【图书馆】自习

【林阳】 操场
  💬 向【王浩宇】发送【下午训练别迟到！】

  ┌─────────────────────────────────────────
  │ 💬 互动消息
  │ 林阳 → 王浩宇
  │ 📨 下午训练别迟到！
  └─────────────────────────────────────────
```

### 文件说明

| 文件 | 说明 |
|------|------|
| `realtime_monitor.py` | 核心监控脚本 |
| `start_monitor.py` | 快速启动脚本 |
| `demo_monitor.py` | 演示脚本（交互式） |
| `monitor.html` | 生成的网页监控文件 |
| `MONITOR_README.md` | 详细使用说明 |

详见 [MONITOR_README.md](MONITOR_README.md)

---

## 五、目录结构

```
D:\致敬传奇AI项目\
├─ README.md                     ← 本文件
├─ caller.py                     ← 小米 API 封装（自动加载 DNS 补丁，支持 max_tokens）
├─ dns_override.py               ← 纯 Python 公共 DNS 解析（绕过本机故障 DNS）
├─ engine.py                     ← 核心引擎：Agent / 场景 / 消息 / 单步 run_step
├─ agent_factory.py              ← 从 JSON 加载角色与场景
├─ schoolAgents.json             ← 校园篇角色设定（9 人，当前主线）
├─ japanAgents.json              ← 日本篇角色设定（旧篇章，供 story_converter 轻小说模式用）
├─ JecAgents.json / malayaAgents.json / AuvAgents.json  ← 其他旧篇章设定
└─ 记录\
   ├─ 人设\                      ← 人设文本档案（302寝室全员、祈的人设等）
   ├─ 原始日志\                  ← 每次运行的角色行动原始记录（.txt）
   ├─ 故事\                      ← 生成的小说（.txt）
   ├─ 程序\
   │  ├─ run_school.py           ← 校园篇：单步记录模式运行（含自动新角色同步）
   │  ├─ story_converter.py      ← 故事转换器（自动适配所有日志 → 小说）
   │  └─ character_generator.py  ← 角色人设生成器（本地/AI）
   └─ 归档\                      ← 旧脚本/旧产出（group_engine.py、run_sim_log.py、日本篇旧故事等，不再使用但可查）
```

---

## 六、核心功能详解

### 1️⃣ 多角色模拟（校园篇 `run_school.py`）

**单步记录模式**：每轮所有角色**依次单独行动**，日志按
`角色名 场景 ：` + 思考/更新记忆/动作 + 场景状态 逐条记录 —— 只记录"每个人干了什么"。

```powershell
& $PY "D:\致敬传奇AI项目\记录\程序\run_school.py" [轮数] [--quiet]
```
- 不传轮数 = 无限跑，Ctrl+C 停止；`--quiet` = 只写日志文件
- 日志默认存到 `记录\原始日志\`，格式 `明远中学_原始记录_时间戳.txt`
- **自动造人**：每轮结束后自动扫描本轮剧情，若出现剧情中不存在的新角色（被角色主动发消息/申请好友的对象），会**实时生成详细人设 → 加入模拟 → 持久化到 schoolAgents.json**，该角色后续轮次按人设正常行动。

### 2️⃣ 故事转换器 `story_converter.py`

自动检测日志格式并选择生成方式，**任何日志都能转**：

| 日志类型 | 自动生成的模式 |
|---|---|
| 校园群体段日志（`第N段|`） | 六人群像小说《302寝室的日常》 |
| 校园单步记录（`名字 场景 ：`） | 六人群像小说（按轮/场景整合 + 心理描写） |
| 日本篇单步记录 | 轻小说精简模式《平行群像物语》 |
| 其他单步（Jec/malaya/Auv） | 小说式排版 |

```powershell
& $PY "D:\致敬传奇AI项目\记录\程序\story_converter.py" <日志路径>            # 自动适配
& $PY "D:\致敬传奇AI项目\记录\程序\story_converter.py" --school <日志>       # 强制校园群像
& $PY "D:\致敬传奇AI项目\记录\程序\story_converter.py" --light-novel <日志>  # 轻小说
& $PY "D:\致敬传奇AI项目\记录\程序\story_converter.py" --novel <日志>        # 小说式
```
功能：自动建立人物关系网（人设/通讯录/消息/同场景）、排除错位关联、消息→回复串联、文学化改写（本地规则，不调 API）。输出到 `记录\故事\`。

### 3️⃣ 角色人设生成器 `character_generator.py`

**推荐流程：优先本地生成，不满意再用 AI 按要求扩写。**

```powershell
# generate：第 1 步本地秒出完整人设；加 --enhance 触发 AI 扩写
& $PY "...\character_generator.py" generate "张伟" --info "17岁，懒散，爱打游戏" --enhance "多写具体事例" [--out schoolAgents.json]

# autofill：纯本地规则补全（不调 API，秒出）
& $PY "...\character_generator.py" autofill "张伟" --info "17岁，懒散，爱打游戏"

# create：AI 全新生成（自动识别老师/教练/学生身份）
& $PY "...\character_generator.py" create "班主任" --info "高二(3)班班主任，英语老师，40岁" --out schoolAgents.json

# complete：AI 补全现有角色人设
& $PY "...\character_generator.py" complete --file schoolAgents.json --name 陈子墨

# scan：扫描剧情日志，发现新角色并一键生成人设写入设定
& $PY "...\character_generator.py" scan <日志路径> [--file schoolAgents.json] [--preview]
```
- 生成的人设遵循项目约定：**普通高中生、不完美、有劣根性、学校禁带手机（有也是偷偷藏）**；成人（老师/教练等）自动切换成人人设模板
- `--preview` 只打印不写入；`--out xxx.json` 直接写入/更新设定文件
- `run_school.py` 已内置自动同步：剧情出现新角色时实时生成并加入模拟（见上）

### 4️⃣ API 与网络适配

- **小米 API**：`caller.py` 调用 `https://api.xiaomimimo.com/v1/chat/completions`，模型 `mimo-v2.5-pro`，Key 在 `caller.py` 顶部（`API_KEY`）。
- **DNS 补丁**：本机路由器 DNS 间歇性故障（域名解析超时），`caller.py` 导入时自动 `dns_override.patch()`，改为走公共 DNS（阿里 223.5.5.5 / 腾讯 119.29.29.29 等）UDP 查询，无需管理员权限。
- **提速**：`caller.call_model` 支持 `max_tokens`；`engine.py` 已给思考/动作（800）与场景更新（400）设上限，避免模型超写拖慢。

---

## 七、数据文件说明

| 文件 | 说明 |
|---|---|
| `schoolAgents.json` | 校园篇 9 人：陈子墨/林阳/王浩宇/赵天磊/孙一凡/周景行 + 班主任/张老师/篮球教练；每人含 persona（详细人设）、初始位置、通讯录 |
| 其他 `*Agents.json` | 日本篇等旧篇章角色设定（story_converter 轻小说模式会读取） |
| `记录\人设\` | 人工整理的人设文本（参考"祈的人设"格式） |
| `记录\原始日志\` | 运行原始记录（可被 story_converter 转小说） |
| `记录\故事\` | 生成的小说成品 |
| `记录\归档\` | 旧脚本/旧产出（不再使用，仅存档） |

### 人设格式约定（schoolAgents.json persona 字段）
```
你是「名字」，X岁，……（身份介绍）。

【性格】……
【劣根性】……   ← 普通高中生的缺点（虚荣/嫉妒/逃避/撒谎/八卦/攀比/小心眼等）
【自我与思考】……
【说话风格】……
【兴趣爱好】……
【对室友】……
【禁忌】……
【记忆】……
【手机】学校明令禁止带手机……  ← 高中生设定必带
```

---

## 八、工作流示例（一条龙）

```powershell
# 1) 跑 4 轮模拟（剧情出现新角色会自动生成人设）
& $PY "D:\致敬传奇AI项目\记录\程序\run_school.py" 4

# 2) 找到最新日志
Get-ChildItem "D:\致敬传奇AI项目\记录\原始日志" -Filter "明远中学_原始记录_*.txt" | Sort LastWriteTime -Descending | Select -First 1

# 3) 转成群像小说
& $PY "D:\致敬传奇AI项目\记录\程序\story_converter.py" --school <日志路径>

# 4) 手动造一个角色（本地优先，AI 扩写兜底）
& $PY "D:\致敬传奇AI项目\记录\程序\character_generator.py" generate "新同学" --info "18岁，转校生，内向" --out schoolAgents.json
```

---

## 九、已知问题与注意事项（重要）

1. **DNS**：本机 DNS 可能超时导致"域名解析失败"，`caller.py` 已自动处理；若手工调试请先 `import dns_override; dns_override.patch()`。
2. **影子进程**：本环境会给后台 Python 派生一个 codex-runtime 影子子进程（命令行相同）。`run_school.py` 已有**进程锁**（`记录\程序\.run_school.lock`，运行时会自动创建/清理），影子进程会自动退出，**不要手动去杀影子进程**（可能牵连主进程）。
3. **长任务**：前台长命令可能被工具中断；跑多轮建议用后台方式（如 `Start-Process`）并轮询日志，或分多次跑。
4. **控制台中文乱码**：PowerShell 控制台（GBK）显示 UTF-8 输出可能乱码，**文件内容是正确的**；如需看内容直接 `Get-Content -Encoding UTF8`。
5. **API 偶发空返回**：小米 API 偶发返回空 content，运行脚本有 4 次重试；仍失败会报错退出，重跑即可。
6. **无断点**：`run_school.py` 单步模式无断点续跑，进程中断后需重跑（旧日本篇断点脚本已移入 `记录\归档\`）。

---

## 十、风格约定（写人设/故事时请遵守）

- **人无完人**：所有人设都要**真实、不完美**，劣根性必须有——这不是"缺点"，而是根植于人性深处的弱点。圣人不存在，每个人都戴着面具生活。
- **劣根性的普遍性**：虚荣、嫉妒、逃避、撒谎、攀比、记仇、自私、功利……这些不是某个群体的专利，而是人类的共性。年龄、身份、职业只是表象，内核都一样。
- **高中生设定补充**：高中**禁带手机**——没手机，或偷偷藏一部从不在人前用。
- **成年人设定补充**：老师也会偏心、也会有私心；父母也会自私、也会控制；领导也会虚伪、也会推卸。不要把任何群体"圣人化"。
- 故事语言风格参考轻小说（语言格式可参考番茄小说《三流人类生存指南》），克制、生活化、带淡淡青春气；不写"三流人类生存指南"字样。
- 原始记录 = 每个人单独干了什么；小说 = 群体叙事整合。


